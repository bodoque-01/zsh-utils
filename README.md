## About
Use this custom zsh function to automatically open a specified Natas level without having to go through the 3 tiring extra clicks to log in into each level.

## Usage
```bash
natas LEVEL_NUMBER LEVEL_PASSWORD
```
## Installatio
```zsh
git clone https://github.com/bodoque-01/zsh-utils.git ~/.zsh/zsh-utils
echo 'source ~/.zsh/zsh-utils/functions.zsh' >> ~/.zshrc
